import { State } from './store'
import { Action } from './actions'

/**
 * @param {State} state
 * @param {Action} action
 * @return {State}
 */

const reducer = (state, action) => {
  return state;
}